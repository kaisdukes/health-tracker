package health.tracker;

import java.nio.file.Path;
import java.nio.file.Paths;

public class TestContext {
    public static final Path BASE_PATH = Paths.get("data");

    private TestContext() {
    }
}